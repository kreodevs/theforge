# Fixtures golden — MDD sanitize (Fase 0)

Pares `*.in.md` / `*.out.md` (o `*.in.json` para `jsonSectionToMarkdown`) usados por `mdd-sanitize.golden.spec.ts`.

Al refactorizar `mdd-sanitize.ts`, estos fixtures deben seguir pasando sin cambiar el output esperado salvo mejora documentada.
