# Plugin utils (web)

Utilidades del Workshop para artifacts de plugins.

| Archivo | Rol |
|---------|-----|
| `pluginApi.ts` | HTTP: artifacts, plugin-data, generación encolada, polling |
| `pluginArtifactContent.ts` | Serialización editor ↔ payload según `contentType` |

Ver `apps/web/src/components/PluginDocPanel.tsx` y `apps/web/src/store/workshopStore.ts` (`pluginData`).
