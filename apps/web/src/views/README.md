# views

Vistas de la aplicación web.

| Componente           | Uso                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **WorkshopView.tsx** | Vista principal: 3 columnas — ChatContainer (**Re-Valorar** → `reassess-complexity` + primer mensaje de entrevista); tabs de documentos; semáforo + costos + auditoría. **Banner `ComplexityPendingBanner`:** si hay propuesta HITL (`complexityPending`), confirmar/descartar o por chat. Header: selector de **etapa** (MDD por `Stage`) + **Nueva etapa** (modal → `createWorkshopStage`); **Ayuda** abre `WorkshopHelpModal` (manual en `content/workshop-manual.md`); **Descargar todo (ZIP)**. **Paso 0:** Benchmark stream → `dbgaContent` + `complexityPending` hasta confirmación; MDD con agentes; Deep Research → `phase0SummaryContent`. **`complexity === LOW`:** oculta pestañas MDD, Blueprint, API; **Generar entregables** usa `generateDeliverablesCascade`. **`complexity === MEDIUM`:** barra filtrada con `isTabVisibleForComplexity` (NEW: sin MDD en barra, insumo Paso 0/Spec; LEGACY: Modificación + MDD); pestañas vacías = borde punteado zinc; con contenido = fondo esmeralda; activa = anillo ámbar. **Legacy:** tab Modificación. Zustand. |

Se entra desde la lista de proyectos en App; se pasa `projectId`, `projectName` y `onBack`.
