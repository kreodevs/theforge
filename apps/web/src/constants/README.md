# constants

- **`legacy-workshop-loading-steps.ts`** — Textos que rotan cada ~6s en **WorkshopView** (panel central) y **ChatContainer** (columna chat) mientras `loadingReason` es `legacy-codebase-doc`, `legacy-mdd` o `legacy-deliverables`. No reflejan eventos reales del API (la generación es una petición larga); sirven de feedback de UX.
