# Stub plugin (desarrollo)

Plugin mínimo para validar el motor genérico en local y CI.

- **Id:** `dev.theforge.stub-plugin`
- **Artifact:** `demo-report` (generatable, JSON)
- **Hook:** `afterDocumentRender` en documento `spec`

No usar en producción. Ver `plugins-enabled/template/README.md` para crear plugins reales.
