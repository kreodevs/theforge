# Skill: excalidraw

Instrucciones para el agente al usar el MCP **user-excalidraw** en Cursor (diagramas hand-drawn animados).

- **SKILL.md** — workflow y reglas obligatorias
- **reference.md** — cheatsheet (colores, plantillas)

Activación: mencionar Excalidraw, diagramas a mano, o invocar la skill `@excalidraw`.

Requisito: MCP `excalidraw` habilitado en Cursor (servidor `user-excalidraw`).
